Prerequisites (Node.js, local Ollama installation, and browser setup)
Installation and configuration (npm run install-all orchestrator script)
Usage instructions (starting backend and frontend concurrently with npm start, using the voice controller, and security override details)
